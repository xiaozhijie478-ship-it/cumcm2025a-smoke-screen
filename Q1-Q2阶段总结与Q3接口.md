# CUMCM 2025 A：问题一至三阶段总结

## 1. 固定口径

- 坐标系、导弹初始状态、无人机初始状态、目标参数均采用官方题面。
- 计算主值采用重力加速度 `g = 9.8 m/s²`；另做 `9.78~9.82 m/s²` 敏感性分析。
- 真目标为下底面圆心 `(0, 200, 0)`、半径 `7 m`、高度 `10 m` 的圆柱体。
- 烟雾云团为半径 `10 m` 的球体，起爆后以 `3 m/s` 匀速下沉，有效期 `20 s`。
- 有效遮蔽采用严格定义：在某时刻，导弹当前位置到真目标圆柱表面每一点的有限线段都必须与有效烟雾球相交。
- 不允许用目标中心代替整个圆柱表面，不允许用无限直线代替导弹到目标点的有限线段。
- 对问题三采用与问题四、五一致的联合口径：对目标表面每一点，只要求至少存在一颗有效烟雾球遮挡相应有限视线段，不要求同一颗球独自遮住整个圆柱。
- 三个单球严格遮蔽时间集合的并集仍是联合遮蔽时间集合的可靠子集，可作为联合优化的基线和认证下界。

## 2. 统一运动学

导弹轨迹为

\[
\mathbf M(t)=\mathbf M_0+300t\,\mathbf e_M,
\qquad
\mathbf e_M=-\frac{\mathbf M_0}{\|\mathbf M_0\|}.
\]

无人机确定航向 `θ` 和速度 `v` 后，保持等高度匀速直线飞行：

\[
\mathbf U(t)=\mathbf U_0+vt(\cos\theta,\sin\theta,0).
\]

第 `i` 枚烟雾弹投放时刻、起爆延迟和起爆时刻分别为 `t_{r,i}`、`τ_i`、`t_{e,i}=t_{r,i}+τ_i`。其投放点和起爆点为

\[
\mathbf Q_i=\mathbf U_0+vt_{r,i}\mathbf e(\theta),
\]

\[
\mathbf E_i=\mathbf U_0+vt_{e,i}\mathbf e(\theta)-\frac12g\tau_i^2\mathbf e_z.
\]

起爆后的云团中心为

\[
\mathbf C_i(t)=\mathbf E_i-3(t-t_{e,i})\mathbf e_z,
\qquad t\in[t_{e,i},t_{e,i}+20].
\]

## 3. 严格遮蔽判据

对圆柱表面点 `P`，定义烟雾球心到有限视线段的距离

\[
D(t,\mathbf P)=
\min_{0\le\lambda\le1}
\left\|
\mathbf M(t)+\lambda[\mathbf P-\mathbf M(t)]-\mathbf C(t)
\right\|.
\]

该时刻完全遮蔽的充要条件为

\[
D_{\max}(t)=\max_{\mathbf P\in\partial\Omega}D(t,\mathbf P)\le10.
\]

令导弹位置为原点、烟雾球为闭球 `B`。能够被该球遮蔽的视线终点集合可写成 `B+cone(B)`：若终点位于该集合，其视线段上存在 `B` 内一点；反之亦然。因此该阴影集合是凸集。圆柱上下两个边缘圆周的凸包恰为整个实心圆柱，故检查两条圆周全部落入阴影集合，等价于检查整个圆柱。算法 C 将圆周上的违反量化为分段二阶三角函数，通过分段边界、驻点和四次方程实根寻找严格最坏点。算法 B 对两个圆周进行高密度采样，并叠加角度误差上界和时间 Lipschitz 上界，独立夹逼真实遮蔽时长。

## 4. 问题一结果

FY1 按题设以 `120 m/s` 朝假目标方向飞行，在 `1.5 s` 投放、延迟 `3.6 s` 起爆：

- 投放点：`(17620, 0, 1800) m`；
- 起爆时刻：`5.1 s`；
- 起爆点：`(17188, 0, 1736.496) m`；
- 严格遮蔽区间：`[8.056445490, 9.448088159] s`；
- 严格遮蔽时长：`1.391642668 s`。

算法 B 的认证时长区间为

\[
[1.390781403,\ 1.392345428]\ \mathrm{s},
\]

算法 B 与算法 C 抽查矛盾数为 `0`。当 `g` 从 `9.78` 变到 `9.82 m/s²` 时，时长从约 `1.36467 s` 变到 `1.41836 s`，因此论文必须声明主值所用的 `g`。

## 5. 问题二结果

采用起爆事件重参数化、差分进化多起点搜索、局部精修、严格 C 重算和 B 认证，当前最佳可行方案为：

- 航向角：`5.185976°`，从 `+x` 轴逆时针计；
- 速度：`140.000000 m/s`；
- 投放时刻：`0.746404047 s`；
- 起爆延迟：`0.175519027 s`；
- 起爆时刻：`0.921923074 s`；
- 投放点：`(17904.068815, 9.445322, 1800.000000) m`；
- 起爆点：`(17928.540892, 11.666417, 1799.849046) m`；
- 严格遮蔽区间：`[0.921923074, 5.509971332] s`；
- 严格遮蔽时长：`4.588048258 s`。

算法 B 对该方案给出的时长夹逼为

\[
[4.587535858,\ 4.588222504]\ \mathrm{s},
\]

且 B、C 在 `301` 个抽查时刻上矛盾数为 `0`。

对单段连续遮蔽，使用起始时刻区间分支、McCormick 松弛和二阶锥必要条件排除了 `4.589000 s`：

\[
4.588048258
\le T^*_{\mathrm{continuous}}
<4.589000000.
\]

相应最优性间隙小于 `0.000951742 s`。这一上界是数值锥规划认证，不是符号证明；若将目标理解为多个分离区间的累计时长，还依赖单弹遮蔽区间连通性。`12000` 组随机物理可行方案审计未发现多段遮蔽，但该性质尚未获得解析证明。

## 6. 问题三官方要求与新增约束

问题三要求 FY1 投放 `3` 枚烟雾干扰弹干扰 M1，并填写官方 `result1.xlsx`。同一架 FY1 在任务开始时只确定一次航向和速度，之后不再调整，因此三枚弹共享 `θ` 和 `v`。

新增约束为

\[
t_{r,2}-t_{r,1}\ge1,
\qquad
t_{r,3}-t_{r,2}\ge1.
\]

建议问题三的独立变量写为

\[
\mathbf x=
(\theta,v,t_{r,1},\tau_1,t_{r,2},\tau_2,t_{r,3},\tau_3),
\]

或使用等价的三个起爆事件参数化，并通过共享航向、共享速度和投放间隔约束保持物理可行。

对第 `i` 枚烟雾弹，定义

\[
d_i(t,\mathbf P)=\operatorname{dist}\!\left(C_i(t),[M(t),\mathbf P]\right).
\]

令时刻 $t$ 仍有效的烟雾球集合为 $A(t)$，问题三的联合观测量和目标为

\[
G_3(t)=\max_{\mathbf P\in\partial\Omega}\min_{i\in A(t)}d_i(t,\mathbf P),
\qquad
\max J_3(\mathbf x)=\operatorname{meas}\{t:G_3(t)\le10\}.
\]

该定义允许相邻两球分别遮住圆柱表面的不同区域。三个单球严格区间的并集只能作为基线，不能再作为完整目标。单架无人机使三个起爆点的水平投影都落在同一条航迹上，因此联合优化仍需满足共享航向、共享速度和投放间隔约束。

## 7. 问题三求解过程与结果

搜索变量取

\[
(\theta,v,t_{r,1},\Delta t_{12},\Delta t_{23},\tau_1,\tau_2,\tau_3),
\qquad \Delta t_{12},\Delta t_{23}\ge1.
\]

为避免人为截断可行域，程序以单位立方体变量覆盖从任务开始到导弹命中前的全部物理可行时序：首投非负、相邻投放至少间隔 `1 s`、起爆不晚于导弹命中、自由落体高度非负。幂变换仅提高早期时序的搜索分辨率，不删除晚期方案。候选搜索使用圆周快速采样，并加入近可行引导项穿过“完全无效时得分恒为零”的平坦区；引导项只存在于差分进化粗搜索。原始单球接力精修只最大化 `meas(S_1∪S_2∪S_3)`；随后新增完整圆柱面的联合目标搜索，直接最大化 $\operatorname{meas}\{t:G_3(t)\le10\}$。

三个独立完整域随机种子经严格 C 精修后分别得到约 `7.614472 s`、`7.616685 s` 和 `7.614472 s`。第一轮活跃集分析在 `v=140 m/s`、`t_{r,1}=0` 上得到延迟起爆接力解 `7.620267 s`。随后增加确定性全角域分箱审计，专门攻击容易被随机种子漏掉的“第一枚投放后立即起爆”分支。分箱法在完整 `0°~360°` 航向域内发现可行盆地，未经人工最优解热启动的粗解为 `6.674324 s`；固定首枚延迟为零后做三维边界精修，得到 `7.650399 s`，与下述最好值只差约 `6.6×10^{-6} s`。这形成“八维完整域随机搜索—四维接力精修—三维立即起爆分支审计”的三层求解链；后两枚起爆时刻均由前一枚严格区间右端点递推，且每一步重新检查投放间隔。

另行测试的锥松弛动态上界约为 `11.88 s`，与当前可行解仍相差过大，不能用于宣称完整八维域近全局最优。新增参数盒外松弛对搜索反复命中的核心活跃分支 `v=140 m/s, t_{r,1}=tau_1=0` 做了完整五维定界：保留航向、后两枚投放时刻和后两枚延迟的全部物理可行范围，同时允许三球联合遮蔽。采用 `0.05/0.005 s` 多分辨率时间格、上下边缘圆周共 `16` 个必要见证点、`14376` 次分支及 `1e-8 m` 距离保护量，得到

\[
7.650405706\le J_{3,\mathrm{core}}^*\le7.900000000\ \mathrm{s}.
\]

绝对间隙为 `0.249594294 s`，按上界归一化约为 `3.16%`。全部开放参数盒均被剪除，认证记录为 `target_certified=true`。该上界覆盖核心分支内任意后两弹时序及联合遮蔽，但不覆盖低速、首弹延迟投放或延迟起爆的互补域。完整八维参数盒外松弛在 `5000` 盒后仍为 `13.942236 s`，弱于原锥松弛，因此完整域仍保留约 `11.88 s` 的上界并明确全局最优性未证。

进一步对反复出现的活跃接力分支做参数剖面：速度固定在上界、首弹立即投放起爆、后两弹在前一严格区间右端点起爆。当前点沿航向和两个延迟方向均表现为单侧或双侧极大；航向左右偏移 `0.01°` 时分别下降约 `0.000526 s` 和 `0.039307 s`，第二枚延迟正负扰动 `0.005 s` 分别下降约 `0.000304 s` 和 `0.022985 s`，第三枚延迟正负扰动 `0.005 s` 均下降约 `0.000015 s`。结合确定性全角域分箱审计，可把 `7.6511 s` 作为这一活跃接力分支的工程性近似上界，分支内数值差距约 `6.9e-4 s`。该数值依赖已识别活跃分支，不能替代完整八维问题约 `11.88 s` 的安全上界，也不能写成全局最优性证明。

当前最佳可行方案为：

- 公共航向角：`179.847029912°`，从 `+x` 轴逆时针计；
- 公共速度：`140.000000000 m/s`；
- 第 1 枚：投放时刻 `0 s`，延迟 `0 s`，起爆时刻 `0 s`，严格区间 `[4.847156695, 7.403159406] s`，单弹时长 `2.556002712 s`；
- 第 2 枚：投放时刻 `2.461855597 s`，延迟 `4.941303809 s`，起爆时刻 `7.403159406 s`，严格区间 `[7.403159406, 10.868067803] s`，单弹时长 `3.464908397 s`；
- 第 3 枚：投放时刻 `5.009571463 s`，延迟 `5.858496340 s`，起爆时刻 `10.868067803 s`，严格区间 `[10.868067803, 12.497562400] s`，单弹时长 `1.629494597 s`。

相邻投放间隔分别为 `2.461855597 s` 和 `2.547715866 s`，均满足至少 `1 s` 的要求。第一、二段端点之间存在约 `6.7×10^{-12} s` 的求根级间隙，严格累计时长已将其扣除：

\[
S_1\cup S_2\cup S_3
=[4.847156694710,\ 7.403159406342]
\cup[7.403159406348,\ 12.497562400361]\ \mathrm{s},
\]

累计严格遮蔽时长为

\[
J_3=7.650405706\ \mathrm{s}.
\]

算法 C 的扫描步长从 `0.05 s` 缩小到 `0.002 s` 时，累计时长只变化约 `10^{-12} s`。算法 B 使用每条圆周 `16384` 个采样点和 `2×10^{-5} s` 时间阈值，独立给出的三弹并集认证区间为

\[
7.648372650\le J_3\le7.651095452\ \mathrm{s},
\]

并且在 `1203` 个抽查时刻上没有发现 B、C 判定矛盾。三个起爆高度依次为 `1800.000000 m`、`1680.359232 m` 和 `1631.822301 m`，其余运动学与有效期约束也全部满足。将全部控制量四舍五入到三位小数后，严格累计时长仍为 `7.649400 s`；在固定策略下采用 `g=9.78`、`9.80665` 和 `9.82 m/s²` 时，累计时长分别为 `7.626906 s`、`7.650191 s` 和 `7.649730 s`，因此仍须声明主值采用 `g=9.8 m/s²`。

在此基础上，使用完整圆柱面联合判定器重新审计当前策略。时间步长 `0.002 s`、8322 个目标表面点的覆盖阶数分解得到：单球充分时长约 `7.650418219 s`，仅双球有效时长为 `0`，仅三球有效时长为 `0`。即当前方案本身没有隐藏的联合新增时间。

进一步进行了两类联合目标优化：八维完整域联合搜索使用随机种子 `31`、`97`，均未超过当前方案；针对相邻烟雾球“旧球补边、新球为未来留位置”的联合交接分支，快速种子 `41`、`137` 的完整表面复核值分别约为 `7.565004 s` 和 `7.539765 s`，高预算种子 `353` 的复核值约为 `7.622369 s`，也均低于当前值。因此正式策略和 `result1.xlsx` 暂不变更。

上述结果现在应表述为：在统一的 $\forall P\exists i$ 联合遮蔽目标下，经完整域搜索、联合交接专项搜索和独立认证得到的当前最佳可行解。现有证据仍不足以证明八维非光滑问题的全局最优。

## 8. 可复现文件

- `q1_strict_occlusion.py`：问题一与算法 B/C 几何底座。
- `q2_optimize.py`：问题二多起点候选搜索和严格重算。
- `q2_global_search.py`：问题二事件参数化、端点精修与连续遮蔽上界。
- `q3_optimize.py`：问题三共享航迹、三次投放、严格区间基线和完整圆柱面联合优化。
- `q3_global_audit.py`：问题三立即起爆分支的确定性全角域分箱审计。
- 官方模板：`../tmp/pdfs/cumcm2025-official/result1.xlsx`。
