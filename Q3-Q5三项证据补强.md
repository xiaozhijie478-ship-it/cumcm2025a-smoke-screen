# Q3--Q5 三项证据补强

## 1. 统一口径

本文仍采用有限视线段和完整圆柱表面的联合遮蔽判据

\[
G_k(t)=\max_{P\in\partial\Omega}\min_{b\in A(t)}
\operatorname{dist}\!\left(C_b(t),[M_k(t),P]\right),
\qquad G_k(t)\le 10.
\]

以下区分三类结论：严格全域上界、给定方案的认证区间，以及依赖活跃分支的工程性近似上界。三者不得在论文中混写。

## 2. Q3：上界证据分层

当前可行值为

\[
J_3=7.650405706\ \mathrm{s}.
\]

算法 B 对该给定方案本身的真实累计时长给出

\[
7.648372650\le J_3(\hat x)\le7.651095452\ \mathrm{s}.
\]

这个区间只认证给定方案的数值评价误差，不自动成为整个八维问题的最优性上界。后续完整域分解、起爆顺序分域与共同参数格削减将安全上界收紧到向外取整的 10.351 s，但仍不足以支持全局近优主张。

不过，完整域搜索反复命中同一个核心活跃结构：速度取 140 m/s、首弹立即投放并起爆。固定这三个活跃约束后，仍对航向、后两枚投放时刻和后两枚起爆延迟组成的完整五维物理域做参数盒分支定界。定界器没有假定三枚烟雾弹分别独立遮蔽，而是允许任意两球或三球共同覆盖圆柱表面的不同区域。

对每个参数盒和时间格，程序用 16 个上下边缘圆周见证点构造完整遮蔽的必要条件；用云心三维包围盒和四个自适应空间子盒包住盒内全部可能位置；再用 303 m/s 的时间 Lipschitz 上界扩大烟雾半径。程序甚至允许不同时间格在参数盒内选择不同参数，因此得到的是原问题的外松弛。任何真实可行策略的遮蔽时间都不会超过该外松弛计数。

采用 0.05 s 粗时间格，在盒上界降至 11 s 后切换到 0.005 s 细时间格；每条上下边缘圆周取 8 个见证点；距离和事件边界分别加入 $10^{-8}$ m 与 $10^{-12}$ s 的单侧保护量。经过 14376 次参数盒分裂后，14377 个终端盒的上界均不超过 7.9 s，开放盒归零并得到 `target_certified=true`。因此核心活跃分支得到

\[
\boxed{7.650405706\le J_{3,\mathrm{core}}^*\le7.900000000\ \mathrm{s}}.
\]

上下界绝对差为 0.249594294 s；按上界归一化的相对间隙约为 3.16%。这是带保护量的双精度数值上界，不是外向舍入区间算术意义上的形式化证明。24 组当前解邻域的随机可行策略反例测试全部满足“外松弛上界不低于密集曲面评价”，最小余量为 0.178866 s。把当前点盒的时间格从 0.01 s 依次缩小到 0.005 s 和 0.002 s 时，其外松弛值由 8.01 s 降到 7.83 s 和 7.722 s，表明剩余误差会随时间格和参数盒继续细化而收敛。

完整八维域也用同一定界器进行了 5000 盒攻击，但由于大参数盒被允许在不同时间格重新选择速度和首弹时序，外松弛仍停留在 13.942236 s 的纯运动学可达上限。后续改用共享速度重参数化、贡献弹数与起爆顺序分解以及自适应共同参数格，把完整域上界收紧到向外取整的 10.351 s。因此不能把 7.90 s 写成完整八维问题的上界，并且仍须明确全局最优性尚未证明。

对更窄的“后两弹恰在前一严格遮蔽段结束时接力”分支进行局部参数剖面得到：

- 航向从当前值向左偏移 0.01 度，时长下降约 0.000526 s；
- 航向向右偏移 0.01 度，时长下降约 0.039307 s；
- 第二枚延迟减少 0.005 s，时长下降约 0.022985 s，增加 0.005 s 时下降约 0.000304 s；
- 第三枚延迟在当前点附近呈平滑极大，正负扰动 0.005 s 均下降约 0.000015 s。

因此可以把

\[
7.6511\ \mathrm{s}
\]

作为该窄接力分支的工程性近似上界使用，并把分支内数值差距写成约 \(6.9\times10^{-4}\) s；但它既不是上述完整五维核心分支的安全上界，也不是完整八维问题的严格上界。论文中最稳妥的表述是：“当前最好可行解在已识别接力分支上达到数值近优；核心五维分支内，带保护量外松弛给出 7.90 s 上界；完整八维域向外取整统一安全上界为 10.351 s，但全局最优性仍未证明。”

## 3. Q4：联合遮蔽收益与机会成本

定义双球的纯空间协同收益

\[
\Delta_{ij}=T_{ij}^{\rm joint}-T_{ij}^{\rm single\ union}.
\]

三档网格复核均发现正协同。较细网格结果为：

| 组合 | 联合总时长 / s | 单球区间并集 / s | 纯协同增益 / s |
|---|---:|---:|---:|
| FY1+FY2 | 4.821190 | 4.712942 | 0.108248 |
| FY2+FY3 | 5.195442 | 4.990505 | 0.204937 |

所以“联合遮蔽没有作用”是不准确的；两球确实可以分别负责圆柱表面的不同区域。问题在于，为了把两颗球同时送入有利的几何位置，会牺牲更大的独立遮蔽时长。

独立累计最优中，FY1+FY2 可贡献约 8.568741 s，FY2+FY3 可贡献约 7.147083 s。相对于这些基线：

| 组合 | 得到的协同 / s | 牺牲的累计总时长 / s | 净结果 |
|---|---:|---:|---:|
| FY1+FY2 | 0.108248 | 3.747551 | 明显为负 |
| FY2+FY3 | 0.204937 | 1.951641 | 明显为负 |

早期 FY1+FY2 协同方案能形成约 4.821 s 的连续段，相比当前方案最长连续段 4.588 s 增加约 0.233 s；但它大幅损失累计总时长。因此，当主目标是累计遮蔽时长时，应保留 11.735131 s 的独立时间窗调度；若另设“最长连续段”为次目标，才有理由采用早期协同方案。

Q4 的准确结论应为：联合遮蔽存在正的几何协同，但当前题设下其机会成本远大于协同增益，故累计目标的净收益为负。

## 4. Q5：13 弹方案与连续时空域认证升级

当前正式方案使用 13 枚烟雾弹：FY1、FY2、FY3 分别使用 3、2、2 枚干扰 M1；FY4 的 3 枚弹分别干扰 M2、M1、M3；FY5 使用 2 枚干扰 M3、1 枚干扰 M2。该方案不是把各枚烟雾弹的单独时长相加，而是始终用统一联合判据重算全部有效烟雾球：

\[
G_k(t)=\max_{P\in\partial\Omega}\min_{b\in A(t)}
\operatorname{dist}\!\left(C_b(t),[M_k(t),P]\right).
\]

时间步长 0.005 s、4514 个目标表面点的密网格评价为：

| 指标 | 离散评价 / s |
|---|---:|
| M1 累计遮蔽 | 23.950139827944 |
| M2 累计遮蔽 | 7.499774830204 |
| M3 累计遮蔽 | 9.241581227614 |
| \(J_{\mathrm{sum}}\) | **40.691495885762** |
| \(J_{\mathrm{min}}\) | **7.499774830204** |
| \(J_{\mathrm{all}}\) | **5.615695576217** |

以上是固定方案的高分辨率离散事实，不是精确真值。13 枚弹的留一损失全部为正；三位小数回代以及 $g=9.79,9.80,9.81\ \mathrm{m/s^2}$ 的扰动复核也已完成。

### 4.1 空槽攻击与多起点审计

优化器先在每架无人机最多 3 枚弹的约束下搜索空槽，把新候选分别指向 M1、M2、M3；接受候选后再执行逐弹留一消融，删除联合方案中边际为零的烟雾弹。最终真正保留的关键新增来自 FY2 和 FY4 对 M1 的补充覆盖。随后从最终方案出发完成 FY1--FY5 的整机分块精修，所有候选均未通过接受阈值。

对 FY3、FY4、FY5 分别使用种子 41、137、353，共完成 9 次独立扰动重启。`q5_final_multistart.json` 已把全部 9 次结果逐条持久化到 `attempts`，而 `history` 保存每个模块三次中的最好候选；三个模块的最好改变量分别为 -0.118854670882、-6.790566186420、-6.484746738631 s，均未被接受。这支持“当前最佳可行解在已有攻击下稳定”，不支持“全局最优”。

### 4.2 联合遮蔽只能先作密网格诊断

把分配给各导弹的单球完整遮蔽区间分别求并，再与统一多球判据的密网格结果比较，联合新增约为：

| 导弹 | 单球并集 / s | 联合评价 / s | 联合新增近似 / s |
|---|---:|---:|---:|
| M1 | 22.442718710490 | 23.950139827944 | 1.507421117454 |
| M2 | 7.132893149665 | 7.499774830204 | 0.366881680539 |
| M3 | 9.118667794195 | 9.241581227614 | 0.122913433419 |
| 合计 | 38.694279654350 | 40.691495885762 | **1.99721623** |

这 1.99721623 s 只称“密网格联合新增近似”，不能称连续域认证增益。它的作用是证明多球分区覆盖在数值上确实重要，而不是替代后续连续认证。

### 4.3 连续下界的正确认证方法

认证器对每个时间块 \(I=[t_0-h,t_0+h]\) 和圆柱曲面小块使用

\[
d_b(P_0,t_0)+L_{b,P_0}h+\rho\le10
\]

作为某颗烟雾球在整个时间块内覆盖整个曲面小块的充分条件，其中 \(\rho\) 是小块的几何覆盖半径。不同小块可以选择不同烟雾球，因此仍允许多球分别覆盖目标的不同区域。程序同时保证：

1. 时间块不得跨越烟雾球起爆或失效事件；
2. 只有在整个时间块内均有效的烟雾球才能参与证明；
3. 双精度判定留出 $10^{-8}$ m 单侧保护量，贴阈值片段记为未决；
4. 已覆盖、已排除、未决三类片段必须无重叠地回填候选域；
5. 只有通过上述充分条件的片段才计入连续域下界。

旧流程曾把由采样根生成的单球候选区间直接计入下界，但这些候选没有逐时间块证明完整圆柱表面均被覆盖，因此旧下界已经撤销。修正后的分导弹证书已经合并，并通过完整分区、输入哈希和下界集合包含检查：

\[
J_{\mathrm{sum}}(\hat x)\ge 40.144267859831\ \mathrm{s}.
\]

### 4.4 固定方案的连续上界

外侧认证器把密网格有效区间作为“可能有效区间”提示，对其补集上的完整时间轴逐块证明无效；不能排除的端点缓冲全部计入上界。时间容差 0.00025 s、最终曲面单元上限 30000 时得到：

| 指标 | 连续域数值下界 / s | 连续域数值上界 / s |
|---|---:|---:|
| M1 | 23.734550386674 | 23.952130001401 |
| M2 | 7.343790580860 | 7.500191752063 |
| M3 | 9.065926892297 | 9.243092786511 |
| $J_{\mathrm{sum}}$ | **40.144267859831** | **40.695414539975** |
| $J_{\mathrm{min}}$ | **7.343790580860** | **7.500191752063** |
| $J_{\mathrm{all}}$ | **5.348435792648** | **5.617196875029** |

所以当前正确的证据表述是

\[
\boxed{40.144267859831\le
J_{\mathrm{sum}}(\hat x)\le40.695414539975\ \mathrm{s}}.
\]

夹逼宽度为 0.551146680144 s，密网格值距连续上界仅 0.003918654213 s。该上界只针对当前固定 13 弹方案，不能写成问题五完整决策域的全局上界。上下界认证都使用带单侧保护量的双精度浮点数值计算，而非外向舍入区间算术，因此最终结果应称“连续域数值证书”，不能称“形式化计算机证明”。
