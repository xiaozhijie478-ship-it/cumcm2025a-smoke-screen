# Q3 严格优化阶段总结（2026-08-28）

## 1. 本轮目标与结论

本轮只处理 Q3，目标分为两层：

1. 继续攻击当前可行解 $7.650405706\ \mathrm{s}$；
2. 尝试把完整八维域的安全数值上界压到 $8.0\ \mathrm{s}$。

三个新的联合/接力多起点搜索均未超过当前方案。当前正式可行解保持不变：

\[
J_3(\hat x)=7.650405706\ \mathrm{s}.
\]

完整八维域 $8.0\ \mathrm{s}$ 目标尚未闭合，不能写成新的正式全局上界。本轮得到的正式新增证据是一个不依赖单球有效集连通性的单弹累计上界：

\[
\boxed{J_{3,\text{one bomb}}^*\le 7.0\ \mathrm{s}}.
\]

证书使用 0.02 s 时间格、上下边缘各 16 个必要见证点和每球 4 个中心子盒；512 次参数盒分裂后开放盒归零，`target_certified=true`。记录见 `q3_single_cumulative_target70.json`。

## 2. 可行解攻击

### 2.1 当前方案复核

- 解析单球接力并集：$7.650405706$ s；
- 完整多球联合密算：$7.650388199$ s；
- 二者差约 $1.75\times10^{-5}$ s。

当前方案几乎没有依赖隐藏的双球或三球纯协同增益。

### 2.2 新多起点结果

- 联合接力三个新种子的粗目标约为 7.669 s，但严格复核仅约 7.54--7.57 s；
- 完整联合两个新种子严格复核约为 7.649254 s；
- 所有候选均低于当前正式解。

粗目标存在约 0.1 s 量级的虚高，今后候选接受必须经过严格圆柱复核。

## 3. 定界器新增能力

`q3_upper_branch_bound.py` 新增：

1. 子盒继承父盒安全上界，防止自适应子盒使队列上界数值回升；
2. 可选单弹累计上界；
3. 必要贡献弹约束 $t_{r,i}+\tau_i\le13.942236250$；
4. 32 份航向角分片；
5. 两弹专用物理域；
6. 固定球心子盒贯穿全时间轴；
7. 固定起爆时刻子盒贯穿全时间轴；
8. 球心—起爆子盒联合枚举；
9. 公共航向、公共速度、竖直自由落体关系和投放间隔的必要兼容性检查；
10. $10^{-4}$ 路线兼容保护量和近立即起爆保护分支。

这些条件均作为外松弛的必要条件使用；未闭合运行只作为诊断，不能冒充目标上界证书。

## 4. 角域与两弹诊断

把航向分为 32 个 $11.25^\circ$ 区间后，每个必要贡献弹配对都有 16 个角区间的根上界不超过 8.0 s。困难区间集中在约 $90^\circ$--$270^\circ$。

对包含当前最优航向的两弹分片 $[168.75^\circ,180^\circ]$：

- 1000 个节点；
- 4 个球心子盒；
- 4 个起爆时刻子盒；
- 公共路线兼容性开启；
- 当前开放盒上界为 $11.35$ s；
- `target_certified=false`。

该数值只表示该分片在当前节点预算下的安全诊断上界，不是完整两弹域上界。记录见 `q3_upper_two_route_guarded_theta16875_180_nodes1000.json`。

## 5. 安全性攻击

路线兼容过滤初版曾在近立即起爆方案上产生低估，原因是从 17.8 km 级坐标中相减恢复毫米级水平位移时出现相消误差。该初版结果已作废且未保留为证书。

修复后采用：

- 起爆时刻不超过 $10^{-3}$ s 时跳过可选路线过滤；
- 公共速度/航向区间比较加入 $10^{-4}$ 单侧保护量；
- 40 组较细物理方案交叉检查；
- 200 组随机域与当前解邻域攻击。

所有 240 组方案均满足“外松弛值不低于对应可行评价”，最小余量为数值零。程序自检增加了近立即起爆回归案例。

## 6. 为什么尚未达到 8.0 s

当前最坏盒已经集中到窄航向、高速度和两枚早期起爆弹。旧变量

\[
(\theta,v,t_{r,1},t_{r,2},t_{r,3},\tau_1,\tau_2,\tau_3)
\]

仍会在区间运算中丢失 $v(t_r+\tau)$、公共航向和自由落体高度之间的相关性。增加空间子盒可以继续降低上界，但计算成本快速增加。

下一轮应重参数化为

\[
(u_x,u_y,e_1,e_2,e_3,\tau_1,\tau_2,\tau_3),
\]

其中

\[
u_x=v\cos\theta,\qquad u_y=v\sin\theta,\qquad e_i=t_{r,i}+\tau_i.
\]

球心可直接写成

\[
C_i(t)=
\begin{pmatrix}
U_{0,x}+u_xe_i\\
U_{0,y}+u_ye_i\\
U_{0,z}+3e_i-\frac12g\tau_i^2-3t
\end{pmatrix}.
\]

该变量体系能在参数盒层面保留三枚弹的公共速度分量，是继续压低两弹和完整三弹上界的优先方向。

## 7. 复现命令

单弹无连通性累计上界：

```powershell
python q4_upper_branch_bound.py --dt 0.02 --n-phi 16 --target 7.0 --max-nodes 3000 --center-cells 4 --names FY1 --time-range 0,13.942236249791 --output q3_single_cumulative_target70.json
```

两弹最难角分片诊断：

```powershell
python q3_upper_branch_bound.py --dt 0.05 --fine-dt 0.01 --refine-below 10 --n-phi 16 --target 8.0 --max-nodes 1000 --center-cells 4 --preset two --single-cumulative-cap 7.0 --consistent-center-cells --required-active 1,2 --event-cells 4 --joint-cell-consistency --theta-range-deg 168.75,180 --output q3_upper_two_route_guarded_theta16875_180_nodes1000.json
```

## 8. 当前可写结论

Q3 当前方案仍为 $7.650405706$ s。新增单弹累计上界证明任何超过 8.0 s 的方案不可能只依赖一枚烟幕弹。两弹同航迹定界器已显著收紧，但最难角分片仍有开放盒，因此完整八维域 8.0 s 上界尚未证明。正式论文继续使用原核心五维分支 $7.9$ s 证书，不用本轮未闭合诊断替代完整域结论。
