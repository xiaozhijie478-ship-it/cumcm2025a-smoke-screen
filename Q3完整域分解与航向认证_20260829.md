# Q3 完整域分解与航向认证（2026-08-29）

## 1. 本轮结论

Q3 当前最好可行值保持不变：

\[
J_3(\hat x)=7.650405706\ \mathrm{s}.
\]

本轮的进展不是再报一组启发式搜索结果，而是把完整控制域按“实际参与遮蔽的烟雾弹数量”分解，并逐段建立安全数值上界：

| 覆盖层 | 当前结论 | 证据状态 |
|---|---:|---|
| 只有一弹实际贡献 | \(J\le 7.0\) s | 已闭合 |
| 恰有两弹实际贡献 | \(J\le 8.18\) s | 完整航向域已有统一上界；尚未闭合 8 s |
| 三弹都可能贡献 | \(J\le 11.451\) s | 原始拼接输出 11.450000000000122 s，向外取整；未闭合盒只在 \([168.75^\circ,185.625^\circ]\) |

因此，当前完整 Q3 物理域可写成

\[
\boxed{7.650405706\le J_3^*\le 11.451\ \mathrm{s}}.
\]

这个 11.451 s 上界覆盖完整物理域，但仍较松，不能据此宣称当前方案全局近优。原有核心五维分支证书

\[
7.650405706\le J_{3,\mathrm{core}}^*\le7.9\ \mathrm{s}
\]

仍然是说明当前方案局部结构近优的最强证据。

## 2. 为什么分解没有漏掉方案

把“实际贡献”理解为：去掉该烟雾球后，某些被计入的遮蔽时刻将不再满足完整圆柱联合遮蔽。任意策略只可能有零、一、二或三颗实际贡献球。

1. 零颗贡献时目标值为零；
2. 一颗贡献时，独立单弹累计证书给出 \(J\le7.0\) s；
3. 两颗贡献时，按两颗有效弹的实际投放先后重新编号。它们的投放间隔至少为 1 s；若原来隔着一枚无贡献弹，真实间隔只会更大。允许第三弹完全不参与，并把两颗有效弹放入通用两弹域，是对原问题的放松，因此两弹域上界仍然安全；
4. 三颗贡献时，每颗都必须在导弹到达目标前起爆。程序用 `required_active=(1,2,3)` 只强制三颗弹在评价时域内起爆，却不强制它们真的产生收益，所以该域是“三颗实际贡献”策略的安全超集。

由此，分别控制一弹、两弹和三弹超集，再取三者上界最大值，即可控制完整 Q3 域。

## 3. 连续变量与外松弛

定界器使用

\[
(u_x,u_y,e_1,e_2,e_3,\tau_1,\tau_2,\tau_3),
\]

其中 \((u_x,u_y)\) 是无人机公共水平速度，\(e_i\) 是第 \(i\) 弹起爆时刻，\(\tau_i\) 是投放至起爆的延时，投放时刻为

\[
r_i=e_i-\tau_i.
\]

程序保留速度环带、航向扇区、非负投放时刻、延时上限、相邻投放至少间隔 1 s、起爆时域和共同航迹约束。每个时间格只要不能排除完整圆柱见证点被联合遮蔽，就把整格计入上界；不同时间格还允许选择参数盒内不同的参数。因此它计算的是比真实问题更容易的外松弛，所得时长只会偏大。

当前数值保护量为距离 \(10^{-8}\) m、事件时刻 \(10^{-12}\) s；粗时间格为 0.05 s，临界盒使用 0.005 s，并用相对运动 Lipschitz 界扩大烟雾半径。这里的“认证”指带单侧保护量的可复现数值证书，不等同于外向舍入区间算术的形式化证明。

## 4. 完整航向域扫描

### 4.1 两弹层

先把 \([0^\circ,360^\circ]\) 切成 32 个 \(11.25^\circ\) 航向分片，每片运行 100 个参数盒节点：

- 30 个分片闭合 \(J\le8\) s；
- 开放分片仅为 \([168.75^\circ,180^\circ]\) 与 \([180^\circ,191.25^\circ]\)；
- 后一分片经过 270 个节点后闭合 \(J\le8\) s；
- 前一分片已有 1000 节点安全上界 8.18 s。

所以原来只属于“困难分片”的 8.18 s，现在与其余航向分片的闭合证书拼接后，可以升级为完整两弹域统一上界：

\[
\boxed{J_{3,\mathrm{two}}^*\le8.18\ \mathrm{s}}.
\]

这仍不是 8 s 闭合证书，因为最后一个困难分片还有开放盒。

### 4.2 三弹层

三弹域进一步切成 128 个 \(2.8125^\circ\) 航向分片，每片先运行 100 个节点：

- 119 个分片直接闭合 \(J\le8\) s；
- 左侧 \([157.5^\circ,168.75^\circ]\) 经 329 个节点闭合；
- 右侧 \([185.625^\circ,191.25^\circ]\) 经 88 个节点闭合；
- 最终未闭合区域只剩 \([168.75^\circ,185.625^\circ]\)；
- 初始 128 分片扫描的最大安全上界为 11.600000000000126 s；进一步按起爆顺序分域并自适应加密共同参数格后，拼接上界降为 11.450000000000122 s，正式向外取整为 11.451 s。

因此完整三弹超集满足

\[
\boxed{J_{3,\mathrm{three}}^*\le11.451\ \mathrm{s}}.
\]

## 5. 起爆顺序分解

投放编号满足

\[
r_{i+1}-r_i\ge1.
\]

起爆时刻为 \(e_i=r_i+\tau_i\)。若后投放的第 \(j\) 弹反而不晚于第 \(i\) 弹起爆，即 \(j>i\) 且 \(e_j\le e_i\)，则必有

\[
\tau_i-\tau_j
= (e_i-e_j)+(r_j-r_i)
\ge j-i.
\]

这个必要条件被加入参数盒收缩器。所有两弹策略由两种起爆顺序覆盖，所有三弹策略由六种顺序覆盖；相同起爆时刻位于多个闭分支中，不会遗漏。

### 5.1 两弹难分片结果

在 \([168.75^\circ,180^\circ]\) 内：

- 起爆顺序与投放顺序相反的分支，149 个节点闭合 \(J\le8\) s；
- 顺序一致分支的 \([168.75^\circ,174.375^\circ]\) 已闭合；
- \([174.375^\circ,177.1875^\circ]\) 经 171 个节点闭合；
- 唯一未闭合角落为“顺序一致且航向在 \([177.1875^\circ,180^\circ]\)”；500 节点诊断上界为 8.27 s，但完整难分片仍由更强的既有 8.18 s 上界控制。

### 5.2 三弹中央分片结果

对 \([177.1875^\circ,180^\circ]\) 的六种起爆顺序：

| 起爆顺序 | 当前状态 |
|---|---:|
| 1-2-3 | 自适应共同参数格、100 节点上界 10.35 s |
| 1-3-2 | 100 节点上界 10.65 s |
| 2-1-3 | 100 节点上界 10.40 s |
| 2-3-1 | 500 节点上界 8.255 s |
| 3-1-2 | 169 节点闭合 \(\le8\) s |
| 3-2-1 | 135 节点闭合 \(\le8\) s |

这说明剩余困难主要集中在“较早投放的弹也较早起爆”的顺序，而不是六种顺序同样困难。

### 5.3 最坏盒真实策略攻击

为判断高上界是否对应真实高值策略，新增 `q3_worst_box_attack.py`。它在每个开放盒内直接优化一组固定的航向、速度、三枚弹起爆时刻和延时；同一组参数必须在所有时刻生效，并逐条检查投放间隔、起爆顺序、速度扇区和起爆高度。

| 开放盒 | 安全外松弛上界/s | 盒内最好真实可行值/s |
|---|---:|---:|
| 两弹 1-2 | 8.270 | 3.505 |
| 三弹 1-2-3 | 11.000 | 5.955 |
| 三弹 1-3-2 | 10.650 | 3.994 |
| 三弹 2-1-3 | 10.400 | 5.050 |
| 三弹 2-3-1 | 8.255 | 2.572 |

其中 1-2-3 与 2-1-3 又使用 3 个随机种子和更高预算复核。所有候选的最大约束违反量为零；联合表面采样值与解析单球严格区间并集只相差约 \(10^{-4}\) s，未发现足以解释高上界的隐藏三球协同长段。这不能证明盒内不存在更好策略，但明确支持“主要误差来自逐时刻可重选参数的外松弛”这一诊断。

### 5.4 自适应多时刻共同参数格

现有定界器会让每颗烟雾弹在全部时间格中选择同一个球心子格和起爆时刻子格。将两类子格从 4 提高到 8 可以加强共同参数一致性，但全域固定使用的成本过高，因此新增以下自适应规则：

1. 先用 4 个球心格、4 个起爆格计算粗上界；
2. 仅当盒上界不超过指定阈值时，追加 8×8 的共同参数格计算；
3. 两个安全外松弛取较小值，因此不会误删真实策略。

在 \([177.1875^\circ,180^\circ]\)、1-2-3 顺序的 20 节点消融中：

| 设置 | 上界/s |
|---|---:|
| 4×4 基线 | 12.250 |
| 8 个球心格、4 个起爆格 | 11.500 |
| 全程 8×8 | 10.850 |
| 自适应 4×4→8×8 | 10.900 |

自适应方案在 100 节点时进一步得到 10.350 s。相同 20 节点消融在 1-3-2 与 2-1-3 顺序中分别由 12.25 降至 10.85、由 11.60 降至 10.75，说明收益不是单一盒偶然现象。

对原来共同达到 11.60 s 的两个航向片进行六顺序拼接后：

- \([174.375^\circ,177.1875^\circ]\) 的六顺序最大上界为 11.450000000000122 s；
- \([177.1875^\circ,180^\circ]\) 的六顺序最大上界为 10.650000000000116 s；
- 其余 128 分片中的最大上界为 11.450000000000099 s。

故完整三弹域及完整 Q3 域正式向外取整上界更新为 11.451 s。

## 6. 安全性回归

本轮新增并通过以下检查：

- 真实 7.650405706 s 可行解在其正确起爆顺序分支中保留；
- 同一点在相反顺序分支中被排除；
- 100 个随机三弹物理点均被按实际起爆时刻排序后的分支保留；
- 8×8 共同参数格对当前 7.650405706 s 可行解的外松弛仍不低于真实值；
- 原有 100 个困难航向物理点仍被速度扇区和投放约束保留；
- 目标高于整个评价时域且节点预算为零时，根盒可以正确闭合；
- 两个程序均通过 Python 编译和内置回归测试。

## 7. 证据文件

- `q3_full_domain_certificate_manifest.json`：一弹/两弹/三弹证据拼接与完整航向覆盖清单；
- `q3_validate_certificate_manifest.py`：从保存的六顺序 JSON 自动重建 11.451 s 完整域上界；
- `q3_velocity_domain_audit.py`：完整航向域分片调度器；
- `q3_velocity_domain_nodes100_32.json`：两弹/三弹 32 分片扫描；
- `q3_velocity_three_nodes100_128.json`：三弹 128 分片扫描；
- `q3_upper_velocity_split_tuned_theta16875_180_nodes1000.json`：两弹困难分片 8.18 s 上界；
- `q3_upper_two_theta180_19125_target8.json`：两弹右侧分片闭合证书；
- `q3_upper_three_theta1575_16875_target8.json`、`q3_upper_three_theta185625_19125_target8.json`：三弹外侧闭合证书；
- `q3_two_order_21_theta16875_180_target8.json`：两弹逆起爆顺序闭合证书；
- `q3_two_order_12_theta174375_1771875_target8.json`：两弹顺序一致的中间分片闭合证书；
- `q3_two_order_12_theta1771875_180_nodes500.json`：两弹唯一剩余角落诊断；
- `q3_order_312_theta1771875_180_target8.json`、`q3_order_321_theta1771875_180_target8.json`：三弹两个顺序闭合证书；
- `q3_order_231_theta1771875_180_nodes500.json`：三弹 2-3-1 顺序 500 节点诊断；
- `q3_worst_box_attack_quick.json`、`q3_worst_box_attack_focused.json`：开放盒真实策略攻击与高预算复核；
- `q3_order_123_center4_nodes20.json`、`q3_order_123_center8_nodes20.json`、`q3_order_123_center8_event8_nodes20.json`、`q3_order_123_adaptive_cells_nodes20.json`：共同参数格消融；
- `q3_three_theta174375_1771875_order_*` 与 `q3_order_*theta1771875_180*`：两个关键航向片的六顺序拼接证据。

## 8. 论文可写与不可写

可以写：

1. 当前最好可行值为 7.650405706 s；
2. 完整 Q3 域已有向外取整的统一安全数值上界 11.451 s；
3. 任意至多两颗实际贡献烟雾弹的策略满足 \(J\le8.18\) s；
4. 所有可能超过 8.18 s 的策略必须依赖三颗烟雾弹，并且当前未闭合航向只在 \([168.75^\circ,185.625^\circ]\)；
5. 核心五维活跃分支内仍有 7.9 s 上界和 3.16% 相对间隙。

不能写：

1. 不能写“Q3 全局最优为 7.6504 s”；
2. 不能把完整域 11.451 s 与可行值的宽间隙隐藏掉；
3. 不能把两弹 8.18 s 上界直接当成三弹上界；
4. 不能把 3.16% 写成完整八维域的全局最优性间隙；
5. 不能把带保护量的浮点数值认证称为形式化区间证明。

## 9. 下一步优先级

1. 只攻击三弹 \([168.75^\circ,185.625^\circ]\) 的六个小分片，不再扫描其余已闭合航向；
2. 优先处理起爆顺序 1-2-3、1-3-2、2-1-3，再处理已压到 8.255 s 的 2-3-1；
3. 继续加强时间格之间的共同参数一致性，但必须用同节点消融证明收益；
4. 若算力有限，论文保留 11.451 s 完整域界与 7.9 s 核心分支界，明确二者作用不同，不继续用暴力节点堆积制造伪精度。
