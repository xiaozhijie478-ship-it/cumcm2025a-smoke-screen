# Q3 重参数化严格优化（2026-08-28）

> 后续更新：完整航向分片、贡献弹数与起爆顺序分域已将本报告的困难分片诊断拼接成完整两弹域 8.18 s 上界，并通过自适应共同参数格得到完整 Q3 域 10.351 s 向外取整上界。见 `Q3完整域分解与航向认证_20260829.md`；本文保留算法形成过程。

## 1. 本轮结论

Q3 当前最好可行方案不变：

\[
J_3(\hat x)=7.650405706\ \mathrm{s}.
\]

本轮没有找到更好的可行解，主要成果是显著收紧两弹困难角分片的安全外松弛：

| 定界器 | 参数化 | 根上界 | 1000 节点上界 | 开放盒 |
|---|---|---:|---:|---:|
| 原定界器 | \((\theta,v,r_i,\tau_i)\) | 13.942236 s | 11.350 s | 923 |
| 新定界器 | \((u_x,u_y,e_i,\tau_i)\) | 12.600 s | **8.180 s** | 342 |

比较对象均为两弹域、航向角分片 \([168.75^\circ,180^\circ]\)、1000 个节点、上下边缘各 16 个必要见证点。新结果记录在 `q3_upper_velocity_split_tuned_theta16875_180_nodes1000.json`。

该运行仍有 342 个开放盒，`target_certified=false`，因此 **8.180 s 是当前节点预算下的安全诊断上界，不是“该分片不超过 8 s”的闭合证书，更不是完整三弹 Q3 的全局上界。**

## 2. 为什么要重参数化

旧变量为

\[
(\theta,v,r_1,r_2,r_3,\tau_1,\tau_2,\tau_3).
\]

球心水平坐标需要区间计算 \(v(r_i+\tau_i)\cos\theta\) 和 \(v(r_i+\tau_i)\sin\theta\)，会连续丢失公共速度、公共航向和起爆时刻之间的相关性。

新变量定义为

\[
(u_x,u_y,e_1,e_2,e_3,\tau_1,\tau_2,\tau_3),
\]

其中

\[
u_x=v\cos\theta,\qquad u_y=v\sin\theta,\qquad e_i=r_i+\tau_i.
\]

投放时刻直接恢复为

\[
r_i=e_i-\tau_i.
\]

第 \(i\) 个烟团在时刻 \(t\) 的中心为

\[
C_i(t)=
\begin{pmatrix}
U_{0,x}+u_xe_i\\
U_{0,y}+u_ye_i\\
U_{0,z}+3e_i-\frac12g\tau_i^2-3t
\end{pmatrix}.
\]

这样三枚弹天然共享同一个 \((u_x,u_y)\)，起爆时刻也同时决定活跃区间和水平起爆位置。

## 3. 物理域与约束

新定界器保留以下必要约束：

1. 速度环带：\(70\le\sqrt{u_x^2+u_y^2}\le140\)；
2. 航向角分片对应的二维扇区约束；
3. \(0\le\tau_i\le\sqrt{2U_{0,z}/g}\)；
4. \(0\le r_i=e_i-\tau_i\)；
5. 相邻投放间隔 \(r_{i+1}-r_i\ge1\)；
6. 起爆不晚于导弹到达时刻；
7. 被要求在贡献窗口内活跃的烟幕弹满足 \(e_i\le13.942236250\) s。

所有收缩均只使用必要条件；无法确认不可行的参数盒继续保留，因此不会把真实可行解误删。

## 4. 本轮关键改进

### 4.1 起爆时刻与球心同步分格

旧程序把起爆时刻子格只用于限制 20 s 活跃区间，球心位置仍按完整起爆时刻区间计算。新程序对每个起爆时刻子格重新计算球心盒，使

\[
C_{x,y}=U_{0,x,y}+(u_x,u_y)e_i
\]

在联合遮蔽枚举中保持一致。该改动使 100 节点上界由 12.40 s 降至 10.65 s。

### 4.2 球心子格物理兼容性

从选定的球心子格和起爆时刻子格反推可能的公共速度、延时和投放时刻区间，并检查相邻投放间隔。无法来自同一架无人机航迹的组合被排除。

### 4.3 时间细网格

0.01 s 时间格使用 \(303\ \mathrm{m/s}\) 的安全 Lipschitz 膨胀后，连当前精确物理方案的外界也达到 8.01 s，几乎无法证明 8 s 目标。把临界盒细网格改为 0.005 s 后，该物理点外界降到 7.83 s，具备闭合 8 s 的数值分辨率。

### 4.4 分支顺序消融

显式起爆时刻的分裂会同时收紧活跃区间、水平位置、竖直位置和投放约束，因此给予其更高优先级。相同 1000 节点下，最终安全诊断上界降到 8.18 s。

全变量强分支在 300 节点时可把 8.54 s 降到 8.50 s，但临界区单节点成本上升一个数量级，收益不足，故仅保留为局部诊断选项，不作为正式批量运行默认配置。

## 5. 安全性验证

已完成：

- 新变量点盒与 `Strategy` 烟团中心公式逐时刻一致；
- 100 个随机困难航向物理点均被速度扇区与约束收缩器保留；
- 当前三弹可行方案的新外松弛值不低于 7.650405706 s；
- 20 组较细随机两弹物理方案检查；
- 200 组随机及近立即起爆方案攻击；
- 所有方案均满足“外松弛值不低于对应可行评价”，最小余量为数值零；
- 代码编译、自检和边界回归测试通过。

## 6. 当前能写与不能写的结论

可以写：

1. Q3 当前可行值为 7.650405706 s；
2. 已有独立证书表明单弹累计遮蔽不超过 7.0 s；
3. 新参数化显著降低了两弹困难角分片的外松弛，从 11.35 s 收紧到 8.18 s；
4. 公共速度分量和起爆时刻—球心联动是有效的定界创新。

不能写：

1. 不能写“两弹困难分片已证明不超过 8 s”，因为仍有开放盒；
2. 仅凭本报告当时的 8.18 s 运行不能写成完整两弹域上界；后续报告已用其余航向闭合证书完成拼接；
3. 不能把两弹结果直接当作完整三弹 Q3 上界；第三枚弹可能产生额外联合遮蔽；
4. 不能用本轮诊断替换原有已经闭合的核心五维 7.9 s 分支证书。

## 7. 复现命令

```powershell
python q3_upper_velocity_branch_bound.py `
  --dt 0.05 `
  --fine-dt 0.005 `
  --refine-below 10 `
  --n-phi 16 `
  --target 8.0 `
  --max-nodes 1000 `
  --center-cells 4 `
  --preset two `
  --single-cumulative-cap 7.0 `
  --consistent-center-cells `
  --required-active 1,2 `
  --event-cells 4 `
  --joint-cell-consistency `
  --theta-range-deg 168.75,180 `
  --output q3_upper_velocity_split_tuned_theta16875_180_nodes1000.json
```

## 8. 下一步

Q3 还不能宣布全局认证完成。下一步优先级为：

1. 在当前 8.18 s 分片上继续做可恢复的更大节点运行或针对最坏盒的局部几何界；
2. 将新参数化扩展到完整三弹域，明确三球联合遮蔽不能由两弹域替代；
3. 按航向分片和必要活跃弹组合建立覆盖完整控制域的证书清单；
4. 只有所有分支闭合后，才把新全局上界写入论文正式结论。
