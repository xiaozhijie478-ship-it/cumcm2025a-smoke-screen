# CUMCM 2025 A：前四问评价与第五问建模接口

## 1. 总体判断

前四问已经形成一条完整的技术主线：先固定严格的完整圆柱面遮蔽定义，再解决单弹计算、单弹优化、同机多弹接力和多机联合调度。现有工作不是简单堆叠差分进化，而是包含几何等价化、独立上下界认证、活跃边界审计和可达性证明。

| 问题 | 当前正式结果 | 证据强度 | 论文状态 | 主要剩余风险 |
|---|---:|---|---|---|
| Q1 | $1.391642668$ s | 强 | 可直接入文 | 题面未给 $g$，必须声明采用 $9.8$ m/s²并报告敏感性 |
| Q2 | $4.588048258$ s | 很强 | 可直接入文 | 连续遮蔽上界为数值认证；单弹多段区间尚无解析排除 |
| Q3 | $7.650405706$ s | 强 | 可作为联合目标下当前最好可行解入文 | 联合搜索未发现净增益，但仍无全局最优性证明 |
| Q4 | $11.735130825$ s | 强 | 可作为当前最好可行解入文 | 非凸全局最优未证明，但三球同时缺一不可已被解析排除 |

总体上，Q1、Q2已经比较稳；Q3已经补上与Q4一致的联合覆盖审计和联合目标搜索；Q4经过累计目标纠正、联合覆盖、三球可达性和FY2桥接审计后，逻辑已经闭合。Q3、Q4共同的剩余边界是非凸问题的全局最优性尚未证明。

## 2. 共用建模底座

对导弹 $k$、烟雾球 $b$ 和真目标圆柱表面点 $P$，统一定义

$$
d_{kb}(t,P)=\operatorname{dist}\!\left(C_b(t),[M_k(t),P]\right).
$$

若时刻 $t$ 的有效烟雾球集合为 $A(t)$，则针对导弹 $k$ 的联合遮蔽观测量为

$$
G_k(t)=\max_{P\in\partial\Omega}\min_{b\in A(t)}d_{kb}(t,P).
$$

当且仅当 $G_k(t)\le10$ m 时，真目标对该导弹完全遮蔽。这里必须使用导弹到目标点的有限线段和完整圆柱表面；目标中心点或无限直线均不能代替。

算法C利用烟雾阴影集合的凸性，把完整圆柱检查化为上下边缘圆周的严格最坏点搜索；算法B使用高密度角度采样、角度误差上界和时间Lipschitz上界独立认证。Q1-Q4的主要数值均由两套算法交叉复核。

## 3. 分问题评价

### Q1：固定策略的严格计算

Q1的价值不只在得到 $1.391642668$ s，而在于建立了后续所有问题共用的观测定义和B/C两套算法。算法B认证区间为

$$
[1.390781403,\ 1.392345428]\ \mathrm{s},
$$

且抽查没有发现B、C矛盾。风险是题目未指定重力加速度；固定策略下结果对 $g$ 可见敏感，因此论文必须明确主值采用 $g=9.8$ m/s²。

### Q2：单机单弹优化

Q2通过起爆事件重参数化、多起点全域搜索、局部精修和严格几何重算，把航向、速度、投放时刻和起爆延迟统一纳入优化。当前方案的速度达到上界 $140$ m/s，严格时长为 $4.588048258$ s。

对单段连续遮蔽已有

$$
4.588048258\le T^*<4.589000000,
$$

最优性间隙小于 $9.52\times10^{-4}$ s。这是前四问中最接近“近全局最优认证”的结果，但仍应称数值认证而非符号证明。

### Q3：同机三弹接力

Q3抓住了三枚弹共享同一航向和速度、相邻投放至少间隔1 s的关键结构。通过完整八维搜索、接力活跃集精修和首枚立即起爆分支审计，得到累计时长

$$
J_3=7.650405706\ \mathrm{s},
$$

算法B给出的认证范围为

$$
7.648372650\le J_3\le7.651095452\ \mathrm{s}.
$$

优点是物理约束、边界分支和三位小数舍入稳定性都已检查。现已进一步使用与Q4一致的 $\forall P\exists b$ 联合判定器审计当前方案，并把联合累计时长直接作为八维搜索及低维联合交接搜索的目标。高密度覆盖阶数分解中，仅双球和仅三球有效时长均为0；多个联合搜索候选的完整表面复核值也未超过当前结果。因此该方案不再只是未经审计的保守口径，而是统一联合目标下的当前最好可行解，剩余风险为缺少全局最优性界。

### Q4：多机单弹联合调度

Q4首先纠正了目标函数：题目允许遮蔽不连续，所以主目标是累计并集时长，不是最长连续段。随后使用真正的多球联合判据，并比较了独立布置、两球协同、三球协同和FY2桥接。

解析可达性证明表明，FY1最晚可能贡献时刻为 $13.942236250$ s，FY3最早可能贡献时刻为 $21.357142857$ s，因此“任意两球均失败、必须三球同时成功”的时间集合严格为空。进一步利用

$$
T_{123}=T_{12}+T_{23}-T_2
$$

做FY2桥接优化，也没有找到超过当前方案的净协同增益。当前正式结果为

$$
J_4=11.735130825\ \mathrm{s}.
$$

该问的主要亮点是把搜索负结果升级为结构性解释；主要边界仍是没有整个非凸问题的全局最优性证明。

## 4. 前四问的论文价值

最值得写进摘要和方法部分的不是“用了差分进化”，而是以下四点：

1. 建立有限视线段上的完整圆柱面遮蔽定义，避免目标中心点近似；
2. 利用阴影集合凸性，把完整圆柱验证化为两条边缘圆周的最坏点问题；
3. 用算法B和算法C形成相互独立的数值认证链；
4. 在Q3、Q4中根据共享航迹和可达性界做结构降维，而不是直接堆高维智能算法。

最需要控制的表述有三条：Q3、Q4只能称“当前最佳可行解”；联合搜索未发现增益不等于联合增益理论上不存在；Q1必须声明重力加速度假设。

## 5. Q5与前四问的依赖关系

```text
Q1 严格几何与认证
        ↓
Q2 单机单弹连续优化
      ↙   ↘
Q3 同机多弹  Q4 多机联合
      \       /
       Q5 多机 × 多弹 × 多导弹
```

Q5不是单纯把Q4从3架无人机扩大到5架。它同时继承：

- Q3的一架无人机共享航向、共享速度和投放间隔约束；
- Q4的多烟雾球联合遮蔽和累计并集目标；
- 新增三枚导弹之间的离散任务分配和多目标时间调度。

## 6. Q5的第一版数学模型

三枚导弹的轨迹为

$$
M_k(t)=M_{k0}-300t\frac{M_{k0}}{\|M_{k0}\|},\qquad k=1,2,3,
$$

命中假目标的时间分别约为

$$
t_{\rm hit,1}=66.999171\ \mathrm{s},\quad
t_{\rm hit,2}=63.750381\ \mathrm{s},\quad
t_{\rm hit,3}=60.366473\ \mathrm{s}.
$$

对无人机 $j=1,\ldots,5$，定义一次确定后不再改变的航向和速度

$$
(\theta_j,v_j),\qquad 0\le\theta_j<2\pi,\quad70\le v_j\le140.
$$

第 $r$ 枚烟雾弹的投放时刻、起爆延迟和任务标签分别为

$$
t_{jr}^{\rm rel},\quad \tau_{jr},\quad a_{jr}\in\{1,2,3\},
$$

每架无人机最多投放3枚，并满足

$$
t_{j,r+1}^{\rm rel}-t_{jr}^{\rm rel}\ge1.
$$

主目标采用三枚导弹累计有效遮蔽时长之和：

$$
\max J_5=\sum_{k=1}^{3}\operatorname{meas}
\left\{t:G_k(t)\le10\right\}.
$$

同时报告每枚导弹各自时长和其中的最小值，防止总和掩盖某枚导弹几乎未被干扰；但最小值暂不进入第一目标，除非后续决定采用公平性约束。

烟雾球在物理上可对任意导弹产生遮蔽，不因表格中的任务标签而失效。`result3.xlsx`中的“干扰的导弹编号”记录该弹的主要任务对象；若一枚弹对多枚导弹都有边际贡献，则在论文中另行说明，并按主要贡献归属填写模板。

## 7. 求解路线比较

### A：独立分配基线

先求5架无人机对3枚导弹的15个单弹最优策略，再按单弹时长贪心分配。优点是快、可解释，可形成下界；缺点是忽略同机共享航向和烟雾区间重叠，不能作为最终方案。

### B：候选航迹库加分层调度（推荐）

1. 建立5×3单机单弹兼容矩阵；
2. 为每架无人机保留少量有代表性的航迹模式；
3. 对每条固定航迹枚举三枚弹的任务序列，并连续优化投放和起爆时序；
4. 从每架无人机的候选计划中各选一个，最大化三枚导弹遮蔽并集总和；
5. 对入选方案做全变量联合精修和B/C认证。

该方案把离散分配、共享航迹和连续时序分开处理，计算量可控，也便于解释每架无人机为什么被分配给某枚导弹。

### C：直接全变量元启发式

直接同时优化5组航向速度、最多15枚弹的投放起爆时序和任务标签。它可以作为最终反攻或消融，但维度高、平坦区多、很难证明没有漏掉更好的任务结构，不适合作为第一求解器。

## 8. Q5当前已经完成的接口

已建立三枚导弹、五架无人机和最多15枚烟雾弹的统一评价器。它能够：

- 检查同一无人机共享航向和速度、最多3枚和投放间隔约束；
- 对M1、M2、M3分别计算完整圆柱面的多球联合遮蔽区间；
- 输出三枚导弹各自累计时长、总和与最小值；
- 复现Q4方案对M1的联合累计结果，作为接口自检。

下一步应先求5×3单机单弹兼容矩阵。该矩阵会直接告诉我们每架无人机最适合干扰哪枚导弹，并为后续同机三弹任务序列提供航向和速度热启动。

## 9. 首版5×3单弹兼容矩阵

使用同一完整圆柱面观测量、一个随机种子完成首轮全域搜索，再用更密目标表面和 $0.02$ s 时间步长复核，得到以下单弹累计时长，单位为秒：

| 无人机 | M1 | M2 | M3 | 首选角色 |
|---|---:|---:|---:|---|
| FY1 | 4.588026 | 0 | 0 | M1核心 |
| FY2 | 3.985501 | 3.812424 | 3.627587 | M2核心或通用补位 |
| FY3 | 3.166468 | 2.589117 | 2.950305 | 中后段补位 |
| FY4 | 3.219223 | 3.555281 | 3.271386 | M2/M3灵活补位 |
| FY5 | 3.792115 | 3.103254 | 3.661215 | M3核心或M1补位 |

FY1对M2、M3的零值又使用随机种子137和353重复搜索，仍未得到完整圆柱遮蔽；其最佳时刻距离完全遮蔽边界仍分别相差约72.6 m和412.7 m。这是较强的不适配证据，但尚不是解析不可达证明。

首版矩阵给出的自然骨架是：FY1优先负责M1，FY2优先负责M2，FY5优先负责M3，FY3和FY4根据各导弹的时间空档进行补位。该骨架只是后续同机三弹调度的热启动；不能把15个单弹最优直接拼接，因为同一无人机的三枚弹必须共享航向和速度。
