# CUMCM 2025 A：问题五深挖、联合遮蔽认证与优化收敛审计

## 1. 统一目标与当前正式结果

对导弹 $k$、目标圆柱表面点 $P$ 和时刻 $t$ 的有效烟雾球集合 $A(t)$，定义

$$
G_k(t)=\max_{P\in\partial\Omega}\min_{b\in A(t)}
\operatorname{dist}\!\left(C_b(t),[M_k(t),P]\right).
$$

当且仅当 $G_k(t)\le 10\ \mathrm m$ 时，真目标对导弹 $k$ 完全遮蔽。记

$$
S_k=\{t:G_k(t)\le10\},\qquad k=1,2,3.
$$

问题五的文字没有唯一指定多导弹之间的聚合方式。为避免因目标函数口径不同造成结果不可比，正式论文同时报告三种指标：

$$
J_{\mathrm{sum}}=\sum_{k=1}^{3}\operatorname{meas}(S_k),
\qquad
J_{\mathrm{min}}=\min_{k=1,2,3}\operatorname{meas}(S_k),
\qquad
J_{\mathrm{all}}=\operatorname{meas}\!\left(\bigcap_{k=1}^{3}S_k\right).
$$

- $J_{\mathrm{sum}}$ 表示三枚导弹的总“导弹--时间”遮蔽收益，是本文优化时采用的主目标；
- $J_{\mathrm{min}}$ 表示保护最弱的一枚导弹，用于检查资源分配是否过度失衡；
- $J_{\mathrm{all}}$ 表示三枚导弹在同一时刻均被遮蔽的累计时长，用作协同防护诊断指标。

采用 $J_{\mathrm{sum}}$ 作为主目标的理由是：每减少一枚导弹的一秒有效观测，都产生一单位独立防护收益；同时，官方赛题讲评对问题五采用“无人机--导弹”任务分配并汇总各配对遮蔽时长的近似处理。考虑到优秀展示论文 A196 另采用三枚导弹有效时间集合的交集作为目标，本文保留 $J_{\mathrm{all}}$ 作为第二口径，明确避免跨口径直接比较数值。

当前 11 弹方案在时间步长 $0.005\ \mathrm s$、4514 个目标表面点的稠密网格下得到

| 指标 | 当前值 / s | 解释 |
|---|---:|---|
| $J_{\mathrm{sum}}$ | 35.109172 | 三枚导弹遮蔽时长之和，主优化指标 |
| $J_{\mathrm{min}}$ | 7.499775 | 三枚导弹中最短的累计遮蔽时长 |
| $J_{\mathrm{all}}$ | 1.439349 | 三枚导弹同时被遮蔽的累计时长 |

其中，$J_{\mathrm{sum}}$ 已获得连续时间、连续圆柱表面上的数值认证夹逼

$$
35.067366178\le J_{\mathrm{sum}}\le35.112385634\ \mathrm s.
$$

$J_{\mathrm{min}}$ 与 $J_{\mathrm{all}}$ 当前为稠密网格诊断值，尚未完成与 $J_{\mathrm{sum}}$ 同等级的连续域认证，因此不得写成严格真值或全局最优值。后续若比较不同聚合偏好，应在 $J_{\mathrm{sum}}$、$J_{\mathrm{min}}$ 和 $J_{\mathrm{all}}$ 之间构造 Pareto 对照，而不是混用三种口径。

本文主目标可简写为

$$
J_5=J_{\mathrm{sum}}=\sum_{k=1}^{3}\operatorname{meas}\{t:G_k(t)\le10\}.
$$

当前方案使用 11 枚烟雾弹：FY1、FY2、FY3 分别使用 3、1、2 枚干扰 M1；FY4 使用 1 枚干扰 M2、1 枚干扰 M3；FY5 使用 2 枚干扰 M3、1 枚干扰 M2。时间步长 $0.005\ \mathrm s$、4514 个目标表面点下得到

| 导弹 | 累计遮蔽时长 / s |
|---|---:|
| M1 | 18.367816 |
| M2 | 7.499775 |
| M3 | 9.241581 |
| 合计 | 35.109172 |

三位小数回代结果为 35.044849 s；采用 $g=9.79,9.80,9.81\ \mathrm{m/s^2}$ 时，同一验证网格下分别为 35.050869、35.131242、34.833664 s。所有 11 枚弹的留一损失均为正，范围为 1.629267--4.110151 s。

## 2. 优化增益的来源

| 阶段 | 稠密网格总时长 / s | 相对上一阶段增益 / s |
|---|---:|---:|
| 15 枚专用三弹候选组合 | 25.071466 | -- |
| 删除零边际弹并复用空槽 | 30.206145 | 5.134679 |
| 第一阶段整机分块联合精修 | 33.351187 | 3.145043 |
| 第二阶段整机分块联合精修 | 35.109172 | 1.757985 |

最终方案相对首个 15 弹组合提高 10.037706 s，增幅约 40.04%，同时烟雾弹数量由 15 枚降至 11 枚。核心原因不是增加烟雾弹，而是允许 FY4、FY5 在同一航线上跨导弹复用，并同时优化整架无人机的航向、速度和全部投放/起爆时序。

剩余空槽的多随机种子攻击只找到一枚表观增益约 $8.1\times10^{-4}$ s 的候选弹；留一复算时其边际贡献为 0，故不纳入正式方案。

## 3. 多球联合遮蔽不是网格假象

只合并各枚正式任务烟雾弹的单球完整遮蔽区间，得到约

| 导弹 | 单球并集 / s | 联合评价 / s | 联合新增 / s |
|---|---:|---:|---:|
| M1 | 17.0079 | 18.3678 | 1.3599 |
| M2 | 7.1329 | 7.4998 | 0.3669 |
| M3 | 9.1187 | 9.2416 | 0.1229 |

因此最终值中约 1.85 s 来自“不同烟雾球分别覆盖目标表面的不同区域”，不能用单球完整遮蔽区间的简单并集代替联合判据。

### 3.1 曲面小块上界

固定时刻，令

$$
f(P)=\min_b\operatorname{dist}(C_b,[M,P]).
$$

具有共同端点 $M$ 的两条线段满足

$$
d_H([M,P],[M,Q])\le\|P-Q\|,
$$

故距离函数及其有限个最小值均为 1-Lipschitz：

$$
|f(P)-f(Q)|\le\|P-Q\|.
$$

若曲面小块中心为 $P_0$、几何覆盖半径为 $\rho$，则

$$
\max_{P\text{ in cell}}f(P)\le f(P_0)+\rho.
$$

程序分别对圆柱侧面和上下底面使用自适应小块细分；只有当前上界最大的单元继续二分。

### 3.2 时间片上界

全局粗界 $300+3=303\ \mathrm{m/s}$ 过于保守。对固定目标点 $P$，若

$$
N_b(t)=(C_b(t)-M(t))\cdot(P-M(t))>0
$$

在整个时间片成立，则烟雾球到视线段的最近点不会落在快速移动的导弹端点。此时可采用

$$
L_{bP}=3+
\frac{\|V_M\times(P-M_0)\|}
{\min_{t\in I}\|P-M(t)\|}
$$

作为该点、该烟雾球在时间片 $I$ 上的变化率上界。$N_b(t)$ 为二次函数，其区间最小值可直接计算；若不能排除导弹端点，则回退到 303 m/s 粗界。

对时间片中点 $t_0$、半宽 $h$ 和曲面小块半径 $\rho$，若存在某颗球使

$$
d_b(P_0,t_0)+L_{bP_0}h+\rho\le10,
$$

则该烟雾球在整个时间片内覆盖整个曲面小块。所有小块均通过即可认证该时间片完全遮蔽。

### 3.3 连续域下界与全时间轴上界

认证器现已从“抽取代表片段”升级为完整候选区间递归认证。时间块不得跨越烟雾球起爆或失效事件，只有在整个时间块内均有效的烟雾球才能参与覆盖证明；双精度判定另留出 $10^{-8}$ m 单侧保护量。时间容差取 0.0005 s、最终曲面单元上限取 30000，得到：

| 导弹 | 单球解析并集 / s | 已认证联合新增 / s | 连续域认证下界 / s |
|---|---:|---:|---:|
| M1 | 17.007452461 | 1.318553162 | 18.326005623 |
| M2 | 7.132759937 | 0.366840192 | 7.499600129 |
| M3 | 9.119600359 | 0.122160067 | 9.241760426 |
| 合计 | 33.259812757 | 1.807553421 | 35.067366178 |

再把密网格有效区间整体视为“可能有效区间”，对其补集上的完整连续时间轴逐块证明无效；不能排除的端点缓冲全部计入上界。时间容差 0.00025 s、曲面单元上限 30000 时得到：

| 导弹 | 连续域下界 / s | 连续域上界 / s |
|---|---:|---:|
| M1 | 18.326005623 | 18.369101262 |
| M2 | 7.499600129 | 7.500191752 |
| M3 | 9.241760426 | 9.243092619 |
| 合计 | **35.067366178** | **35.112385634** |

所以当前方案的连续域数值夹逼为

$$
\boxed{35.067366178\le J_5\le35.112385634\ \mathrm s}.
$$

夹逼宽度为 0.045019455 s。密网格值 35.109171990 s 落在区间内，距认证上界约 0.003213644 s。外侧审计没有发现密网格候选区间之外的新有效时间段；上界多出的部分全部来自各区间端点附近无法排除的亚毫秒缓冲。当前实现使用带单侧保护量的双精度浮点，不是带外向舍入的形式化区间证明，因此应称“连续域数值认证夹逼”。

## 4. 独立重启与收敛审计

程序对 FY3、FY4、FY5 分别循环 3 个随机扰动起点，共执行 9 次分块优化；当前 JSON 只保留每个模块三次候选中的最佳一条，而非全部九条逐次日志：

| 模块 | 最好候选相对当前验收值 |
|---|---:|
| FY3 | -0.108394 s |
| FY4 | -6.725751 s |
| FY5 | -6.484746 s |

较大扰动没有找到更优盆地，且 FY4、FY5 的跨导弹折中结构较窄。随后从当前解本身再做一轮小步精修：FY1、FY3、FY5 均下降；FY2 增益 0.002124 s；FY4 增益 0.004388 s。后二者均低于预先规定的 0.005 s 接受阈值，因此正式方案保持不变。

现有证据支持“多起点攻击和局部精修下稳定的当前最佳可行解”，不支持“全局最优解”。

## 5. 论文中可以主张和不能主张的内容

可以主张：

1. 建立了有限视线段、完整圆柱表面和多球分区覆盖的统一联合判据；
2. 11 弹方案在四级时空网格、三位小数回代、重力扰动和留一消融下稳定；
3. 多球联合覆盖至少带来 1.807553 s 的连续域认证增益，密网格估计约为 1.85 s；
4. 分层组合、空槽复用和整机分块精修相对首个 15 弹方案提高约 40.04%；
5. 三个模块各使用三个种子进行扰动重启，模块内最佳候选仍未超过当前方案；当前持久化文件只记录三个模块各自的最佳候选。

不能主张：

1. 35.109172 s 是精确真值；应报告 $35.067366\le J_5\le35.112386$ 的连续域数值夹逼；
2. 当前方案是全局最优；
3. 所有约 1.85 s 的密网格联合新增都已转化为认证下界；当前已认证 1.807553 s；
4. 任务标签会限制烟雾球的物理作用范围。

## 6. 可复现文件

- `q5_optimize.py`：分层候选、空槽复用、整机精修、多起点攻击与验证；
- `q5_block_attacked_plan_v2.json`：当前正式 11 弹方案；
- `q5_block_attacked_validation_v2.json`：四级网格、舍入、重力和边际验证；
- `q5_certify_joint.py`：联合曲面--时间认证器；
- `q5_joint_hybrid_certificate.json`：35.067366 s 连续域下界与逐段分类记录；
- `q5_outer_continuous_certificate_tight.json`：35.112386 s 全时间轴上界记录；
- `q5_joint_targeted_certificate.json`：旧版抽取式认证记录，仅用于展示认证器迭代；
- `q5_multistart_plan.json`：三个模块各三种子的聚合攻击结果；文件保存每个模块的最佳候选，未保存全部九条逐次日志；
- `result3.xlsx`：官方提交模板结果。
