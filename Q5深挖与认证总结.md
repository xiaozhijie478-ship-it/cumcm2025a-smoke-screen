# CUMCM 2025 A：问题五深挖、联合遮蔽认证与优化收敛审计

## 1. 统一判据与证据边界

对导弹 $k$、目标圆柱表面点 $P$ 和时刻 $t$ 的有效烟雾球集合 $A(t)$，定义

$$
G_k(t)=\max_{P\in\partial\Omega}\min_{b\in A(t)}
\operatorname{dist}\!\left(C_b(t),[M_k(t),P]\right).
$$

这里 $[M_k(t),P]$ 是从导弹到目标表面点的**有限线段**。当且仅当

$$
G_k(t)\le 10\ \mathrm m
$$

时，真目标对导弹 $k$ 完全遮蔽。该定义同时保留完整圆柱表面、有限视线段和多球分区覆盖，不能退化为目标中心判据，也不能把目标后方与无限直线相交的烟雾计为有效。

记

$$
S_k=\{t:G_k(t)\le10\},\qquad k=1,2,3.
$$

问题五同时报告三种聚合指标：

$$
J_{\mathrm{sum}}=\sum_{k=1}^{3}\operatorname{meas}(S_k),\qquad
J_{\mathrm{min}}=\min_{k=1,2,3}\operatorname{meas}(S_k),\qquad
J_{\mathrm{all}}=\operatorname{meas}\!\left(\bigcap_{k=1}^{3}S_k\right).
$$

- $J_{\mathrm{sum}}$ 是三枚导弹的总“导弹--时间”遮蔽收益，也是当前优化主目标；
- $J_{\mathrm{min}}$ 检查最弱导弹是否被明显牺牲；
- $J_{\mathrm{all}}$ 衡量三枚导弹同时被遮蔽的协同防护时长。

必须区分三层证据：稠密网格给出当前可行方案的离散评价；连续域认证给出同一固定方案的数值上下界；以上两者都不是整个非凸决策域的全局最优性证明。

## 2. 当前正式 13 弹方案

当前方案共使用 13 枚烟雾弹，分配为：

| 无人机 | 用弹数 | 任务分配 |
|---|---:|---|
| FY1 | 3 | 3 枚均干扰 M1 |
| FY2 | 2 | 2 枚均干扰 M1 |
| FY3 | 2 | 2 枚均干扰 M1 |
| FY4 | 3 | M2、M1、M3 各 1 枚 |
| FY5 | 3 | 2 枚干扰 M3，1 枚干扰 M2 |

在时间步长 $0.005\ \mathrm s$、4514 个目标表面点的稠密网格下，离散评价为：

| 指标 | 当前值 / s | 含义 |
|---|---:|---|
| M1 累计时长 | 23.950139827944 | $\operatorname{meas}(S_1)$ |
| M2 累计时长 | 7.499774830204 | $\operatorname{meas}(S_2)$ |
| M3 累计时长 | 9.241581227614 | $\operatorname{meas}(S_3)$ |
| $J_{\mathrm{sum}}$ | **40.691495885762** | 主目标 |
| $J_{\mathrm{min}}$ | **7.499774830204** | 最弱导弹指标 |
| $J_{\mathrm{all}}$ | **5.615695576217** | 三弹同时遮蔽指标 |

这些数值是当前固定方案的高分辨率离散事实，不应写成精确真值。三位小数回代的 $J_{\mathrm{sum}}$ 为 40.629254654355 s；重力加速度取 $9.79,9.80,9.81\ \mathrm{m/s^2}$ 时，相同验证层级下分别得到 40.673022737227、40.728292987548、40.301901850977 s。13 枚弹的留一损失全部为正，范围为 1.629266847762--4.110151343083 s，说明最终表中没有离散评价下的零边际冗余弹。

## 3. 优化为什么能继续提高

本轮提升来自“攻击空槽”，而不是继续微调已饱和的原有弹：

1. 先在每架无人机最多 3 枚弹的物理约束下枚举尚未使用的投放槽；
2. 对空槽分别尝试 M1、M2、M3，并用多个种子优化其释放和起爆时序；
3. 把候选弹并入全体烟雾球后，用统一多球判据重算三枚导弹，而不是把单弹收益机械相加；
4. 对扩充方案逐弹执行留一消融，删除边际为零的候选；
5. 再对各无人机的共同航向、共同速度和机内全部时序做分块精修。

空槽攻击找到 FY2 和 FY4 面向 M1 的两个关键补充区间。另一个面向 M3 的候选在联合方案中留一边际为零，因而被删除，最终保留 13 枚弹。相对最初 15 枚专用候选组合的 25.071466 s，当前密网格主目标提高约 15.620030 s，增幅约 62.30%。这个结果说明，问题五的关键不只是“单弹放得更准”，而是把有限空槽分配给全局时间轴上的缺口，并允许跨导弹复用同一无人机航线。

## 4. 多球联合遮蔽的作用

对每枚导弹，把分配给它的各颗烟雾弹单独评价，再合并这些单球完整遮蔽区间，可得以下密网格近似：

| 导弹 | 单球区间并集 / s | 联合评价 / s | 联合新增近似 / s |
|---|---:|---:|---:|
| M1 | 22.442718710490 | 23.950139827944 | 1.507421117454 |
| M2 | 7.132893149665 | 7.499774830204 | 0.366881680539 |
| M3 | 9.118667794195 | 9.241581227614 | 0.122913433419 |
| 合计 | 38.694279654350 | 40.691495885762 | **1.99721623** |

这约 1.99721623 s 仅是密网格下的“联合评价减单球并集”近似，用来说明不同烟雾球可分别覆盖圆柱表面的不同区域；它不是连续域认证下界，也不能称为严格协同收益。连续认证必须直接对

$$
\max_{P\in\partial\Omega}\min_b
\operatorname{dist}(C_b,[M_k,P])
$$

进行时空分支判定，不能由采样根或单球候选区间直接替代。

## 5. 连续时空认证方法

### 5.1 曲面小块上界

固定时刻，令

$$
f(P)=\min_b\operatorname{dist}(C_b,[M,P]).
$$

具有共同端点 $M$ 的两条线段满足

$$
d_H([M,P],[M,Q])\le\|P-Q\|,
$$

因此距离函数及有限个距离函数的最小值关于 $P$ 都是 1-Lipschitz：

$$
|f(P)-f(Q)|\le\|P-Q\|.
$$

若圆柱曲面小块中心为 $P_0$、几何覆盖半径为 $\rho$，则

$$
\max_{P\text{ in cell}}f(P)\le f(P_0)+\rho.
$$

认证器分别细分圆柱侧面、上底面和下底面，只继续二分当前上界最大的曲面单元。

### 5.2 时间片充分条件

对时间片 $I=[t_0-h,t_0+h]$，若烟雾球 $b$ 在整个 $I$ 内有效，且其到视线段距离的时间变化率上界为 $L_{bP_0}$，则

$$
d_b(P_0,t_0)+L_{bP_0}h+\rho\le10
$$

可保证该球在整个时间片内覆盖该曲面小块。每个曲面小块允许选择不同烟雾球，因此该条件保留多球分区覆盖。时间块不得跨越起爆或失效事件；贴近阈值的判断留出 $10^{-8}$ m 单侧保护量，不能证明覆盖或排除的片段统一记为未决。

### 5.3 固定方案连续上下界

下界认证只累计通过上述充分条件的时间块；单球候选和联合候选中的未决片段均不计入。时间容差 0.0005 s、曲面单元上限 8000 时得到下界。上界认证再对密网格候选有效区间的补集逐块证明无效，无法排除的端点缓冲全部计入上界；时间容差 0.00025 s、曲面单元上限 30000。最终结果为：

| 指标 | 连续域数值下界 / s | 连续域数值上界 / s |
|---|---:|---:|
| M1 | 23.734550386674 | 23.952130001401 |
| M2 | 7.343790580860 | 7.500191752063 |
| M3 | 9.065926892297 | 9.243092786511 |
| $J_{\mathrm{sum}}$ | **40.144267859831** | **40.695414539975** |
| $J_{\mathrm{min}}$ | **7.343790580860** | **7.500191752063** |
| $J_{\mathrm{all}}$ | **5.348435792648** | **5.617196875029** |

因此当前可以安全写为

$$
\boxed{40.144267859831\le J_{\mathrm{sum}}(\hat x)\le40.695414539975\ \mathrm s}.
$$

夹逼宽度为 0.551146680144 s，约占上界的 1.354%。密网格值 40.691495885762 s 位于区间内，距连续上界 0.003918654213 s。旧版流程把由采样根生成的单球候选区间直接计入下界，这一步没有逐时间块证明连续圆柱表面均被覆盖，故旧下界已撤销。新版流程只累计通过曲面--时间充分条件的片段，并将覆盖、排除、未决三类区间无重叠地回填候选域；聚合器还逐项核对输入哈希、完整时间轴分区、上下集合包含与目标函数重构。

这里的 40.695414539975 s 只约束**当前固定 13 弹方案**，不是整个问题五决策域的全局上界。认证器使用带保护量的双精度浮点计算，而非外向舍入区间算术，所以应称“连续域数值证书”，不能称为形式化证明。

## 6. 多起点和局部收敛审计

对 FY3、FY4、FY5 各使用种子 41、137、353，共完成 9 次独立扰动重启。`q5_final_multistart.json` 的 `attempts` 数组已经逐次持久化全部 9 次候选，而 `history` 记录每个无人机三次候选中的最佳一次及是否接受：

| 模块 | 三次中的最好候选相对该层验收基线 |
|---|---:|
| FY3 | -0.118854670882 s |
| FY4 | -6.790566186420 s |
| FY5 | -6.484746738631 s |

三个最佳候选均未被接受；随后从最终方案出发的一轮 FY1--FY5 小步分块精修也全部被拒绝。现有证据支持“经过空槽攻击、留一剪枝、局部精修和 9 次扰动重启后稳定的当前最佳可行解”，不支持“已找到全局最优解”。

## 7. 论文中可以与不能主张的内容

可以主张：

1. 建立了有限视线段、完整圆柱表面和多球分区覆盖的统一判据；
2. 当前 13 弹方案的密网格评价为 $J_{\mathrm{sum}}=40.691495885762$ s、$J_{\mathrm{min}}=7.499774830204$ s、$J_{\mathrm{all}}=5.615695576217$ s；
3. 单球并集与多球联合评价之差约为 1.99721623 s，但该数值只作密网格协同诊断；
4. 方案通过四级网格、三位小数回代、重力扰动、13 次留一消融、一次全机分块精修和 9 次多起点重启审计；
5. 当前固定方案的 $J_{\mathrm{sum}}$ 连续域数值夹逼为 $[40.144267859831,40.695414539975]$ s，并同时给出 $J_{\mathrm{min}}$、$J_{\mathrm{all}}$ 的集合夹逼。

不能主张：

1. 40.691495885762 s 是精确真值；
2. 约 1.99721623 s 联合新增已经得到连续域认证；
3. 40.695414539975 s 是问题五的全局上界；
4. 当前方案是全局最优；
5. 带保护量的双精度数值证书等同于形式化计算机证明。

## 8. 可复现文件

- `q5_optimize.py`：分层候选、空槽攻击、零边际剪枝、整机精修、多起点攻击和验证；
- `q5_final_plan.json`：当前正式 13 弹方案；
- `q5_final_validation.json`：四级网格、舍入、重力和留一边际验证；
- `q5_final_bomb_table.json`：13 枚烟雾弹的决策量、单球区间与离散诊断；
- `q5_final_empty_attack.json`：空槽攻击过程；
- `q5_final_refined_plan.json`：最终方案整机分块精修记录；
- `q5_final_multistart.json`：9 次扰动重启的 `attempts` 明细和模块最佳 `history`；
- `q5_certify_joint.py`：联合曲面--时间下界认证器；
- `q5_refine_lower_certificate.py`：只对未决边界片段追加更高预算认证的工具；
- `q5_merge_lower_certificates.py`：核对输入谱系并合并 M1/M2/M3 分证书；
- `q5_final_continuous_upper.json`：固定方案 40.695414539975 s 连续域数值上界；
- `q5_final_continuous_lower.json`：固定方案 40.144267859831 s 连续域数值下界；
- `q5_final_aggregate_certificate.json`：汇总并核验三种目标的上下界、分区与哈希；
- `deliverables/result3.xlsx`：官方提交模板结果。
